"use client";
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function AdminLogin() { const router = useRouter(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/support/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(r.ok) router.replace('/admin/support'); else {const d=await r.json();setError(d.error||'Giriş başarısız.');}setLoading(false);}
return <main className="flex min-h-screen items-center justify-center bg-neutral-950 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><div className="mb-8"><p className="text-sm font-semibold text-blue-600">Müşteri destek merkezi</p><h1 className="mt-2 text-3xl font-bold">Yetkili girişi</h1><p className="mt-2 text-sm text-neutral-500">Canlı ziyaretçileri ve konuşmaları yönet.</p></div><label className="text-sm font-medium">E-posta<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 mb-4 w-full rounded-xl border px-4 py-3" required /></label><label className="text-sm font-medium">Şifre<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 mb-4 w-full rounded-xl border px-4 py-3" required /></label>{error&&<p className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white disabled:opacity-50">{loading?'Giriş yapılıyor...':'Panele gir'}</button></form></main>}
